apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: redis-cache-demo
type: Opaque
stringData:
  # Change this value. The app reads it via SPRING_DATA_REDIS_PASSWORD env var.
  redis-password: "changeme123"
